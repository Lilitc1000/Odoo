<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <record id="estate_offer_action" model="ir.actions.act_window">
    <field name="name">Offers</field>
    <field name="res_model">estate.property.offer</field>
    <field name="view_mode">list,form</field>
  </record>

  <record id="estate_property_offer_list_view" model="ir.ui.view">
    <field name="name">estate.property.offer.list</field>
    <field name="model">estate.property.offer</field>
    <field name="arch" type="xml">
      <list editable="bottom" decoration-success="status=='accepted'"
        decoration-danger="status=='refused'">
        <field name="offer_price" />
        <field name="partner" />
        <field name="validity" />
        <field name="date_deadline" />
        <button name="action_offer_accept" type="object" icon="fa-check" string="Accept" invisible="status == 'accepted' or status == 'refused'" />
        <button name="action_offer_refuse" type="object" icon="fa-times" string="Reject" invisible="status == 'accepted' or status == 'refused'" />
        <field name="status" />
      </list>
    </field>
  </record>
  <record id="estate_property_offer_form_view" model="ir.ui.view">
    <field name="name">estate.property.offer.form</field>
    <field name="model">estate.property.offer</field>
    <field name="arch" type="xml">
      <form>
        <sheet>
          <group>
            <field name="offer_price" />
            <field name="partner" />
            <field name="validity" />
            <field name="date_deadline" />
            <field name="status" />
          </group>
        </sheet>
      </form>
    </field>
  </record>
</odoo>